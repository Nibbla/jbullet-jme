<?xml version="1.0" encoding="UTF-8"?>

<!--
    Document   : build-include.xml
    Created on : 5. Januar 2010, 16:38
    Author     : normenhansen
    Description: target to build linux native library
-->

<project basedir=".." default="default" name="bullet-jme-native-linux">

    <target name="_nativelib_linux" if="isLinux">
        <cc name="${native.linux.compiler}" warnings="severe" debug="${compile.debug}" link="shared" outfile="${build.native.dir}/${library.name}" objdir="${build.native.dir}">
           <fileset dir="${source.dir}">
               <include name="*.cpp">
               </include>
           </fileset>
           <includepath path="${native.java.include}"/>
           <includepath path="${native.java.include}/linux"/>
           <includepath path="${native.bullet.include}"/>
           <includepath path="${build.headers.dir}"/>
           <linker name="${native.linux.compiler}">
               <libset dir="${bullet.folder}/src/LinearMath" libs="LinearMath"/>
               <libset dir="${bullet.folder}/src/BulletDynamics" libs="BulletDynamics"/>
               <libset dir="${bullet.folder}/src/BulletCollision" libs="BulletCollision"/>
               <libset dir="${bullet.folder}/src/BulletMultiThreaded" libs="BulletMultiThreaded"/>
           </linker>
        </cc>
        <copy file="${build.native.dir}/lib${library.name}.so" tofile="${dist.dir}/lib${library.name}.so" failonerror="true" verbose="true"/>
    </target>
    
</project>
